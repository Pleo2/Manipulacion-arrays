// Es un metodo que realiza un recorrido dentro de el array
// itera cada uno de lo index de un array
const array = ['Qatar', 'Argentina', 'Venezuela', 'Mexico'];

for (let index = 0; index < array.length; index++) {
  const element = arrayForEach[index];
  console.log (` Mi pais es: ${element}`);
}

// * ForEach
array.forEach(element => {
  console.log (`Mi pais es: ${element}`);
});





